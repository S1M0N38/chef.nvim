================================================================================
INTRODUCTION                                                       *chef*

chef.nvim — Suggestions for your next meal

<!-- TODO: Add a brief introduction to your plugin. Explain what it does
and link to relevant resources. -->

Links:
- Changelog: https://github.com/S1M0N38/chef.nvim/blob/main/CHANGELOG.md
- License (MIT): https://github.com/S1M0N38/chef.nvim/blob/main/LICENSE

Table of contents:

1. FEATURES: What this plugin provides.                       |chef-features|
2. REQUIREMENTS: Plugin dependencies and setup.            |chef-requirements|
3. INSTALLATION: How to install and use the plugin.         |chef-installation|
4. CONFIGURATION: Available options and their defaults.          |chef-config|
5. COMMANDS: Commands provided by the plugin.                  |chef-commands|
6. API: Exposed functions and development interfaces.              |chef-api|
7. DEVELOPMENT: Testing, building, and release workflows.    |chef-development|
8. TROUBLESHOOTING: Common issues and solutions.         |chef-troubleshooting|

================================================================================
FEATURES                                                        *chef-features*

<!-- TODO: Describe the main features of your plugin. -->

================================================================================
REQUIREMENTS                                                *chef-requirements*

<!-- TODO: List the requirements for your plugin. -->

================================================================================
INSTALLATION                                                *chef-installation*

Install using [lazy.nvim](https://github.com/folke/lazy.nvim):

>lua
  {
    "S1M0N38/chef.nvim",
    lazy = false,
    opts = {},
  }
<

================================================================================
CONFIGURATION                                                     *chef-config*

<!-- TODO: Document available configuration options. -->

                                                                 *chef.setup()*
chef.setup({opts}) ~
  Set up the plugin with the given options.

================================================================================
COMMANDS                                                        *chef-commands*

:Chef ~
  Show a floating window with an ASCII art meal suggestion.

:checkhealth chef ~
  Run health checks to verify chef.nvim setup and configuration.

================================================================================
API                                                                  *chef-api*

<!-- TODO: Document the public API. -->

================================================================================
DEVELOPMENT                                                  *chef-development*

Run tests using mini.test via lazy.minit:
>bash
  # Run all tests
  make test

  # Run a single test file
  make test-one MODULE=chef

  # Run explicit file
  nvim -l tests/minit.lua --minitest tests/chef_spec.lua
<

Health Checks: ~

Validate plugin setup and functionality:
>vim
  :checkhealth chef
<

Minimal Reproduction: ~

Debug issues using the included reproduction environment:
>bash
  nvim -u repro/repro.lua
<

Code Formatting: ~

>bash
  # Check formatting
  make lint

  # Auto-format all Lua files
  make format
<

Generate Help Tags: ~

After modifying `doc/chef.txt`, regenerate the tags file:
>bash
  nvim -c "helptags doc/" -c "q"
<

Release Automation: ~

Use conventional commit format for automatic versioning:
- `feat:` for new features (minor version bump)
- `fix:` for bug fixes (patch version bump)
- `BREAKING CHANGE:` for breaking changes (major version bump)

================================================================================
TROUBLESHOOTING                                          *chef-troubleshooting*

First, run the health check to identify issues:
>vim
  :checkhealth chef
<

If you encounter problems, follow these steps to reproduce the issue
in a clean environment:

1. Clone chef.nvim and cd into the directory
2. Run `nvim -u repro/repro.lua`
3. Run `:checkhealth chef`
4. Try to reproduce the issue
5. Report the repro.lua and any error messages

================================================================================
vim:tw=78:ts=8:et:ft=help:norl:
